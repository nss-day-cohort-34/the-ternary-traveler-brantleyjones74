// imports function obj and gives obj variable name events
import events from "./contact-form.js";

// invoking of both functions inside the object named events
events.contactsPostToDom();
events.saveEvent();
